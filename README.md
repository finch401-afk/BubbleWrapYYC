# BubbleWrapYYC Cloudflare deployment

This folder is ready for Cloudflare Workers static asset deployment.

- Deploy command: `npx wrangler deploy`
- Build command: leave blank
- Root directory: repository root

The website entry file is `public/index.html`, and `wrangler.jsonc` tells Cloudflare where the static files are located.
